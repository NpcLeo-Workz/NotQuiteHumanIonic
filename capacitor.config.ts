import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'be.thomasmore.graduaten.notquitehuman',
  appName: 'notquitehuman',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
